# Quick Search UI Cleanup

- [x] Size the dialog and application window so every main control fits without dialog scrolling.
- [x] Make the periodic table and filter columns responsive.
- [x] Allow the cell-length inputs to wrap without overflowing.
- [x] Add validation for numeric ranges and element-count syntax.
- [x] Debounce live restraint queries and handle request failures.
- [x] Add dialog semantics, focus management, and keyboard behavior.
- [x] Cancel discards draft filters; Search saves the criteria used for the next open.
- [ ] Add UI coverage for minimum window size, zoom, and narrow viewports.
