# PXRD survivor review (#51)

The [decision record](pxrd-mutation-review.json) classifies all 78 surviving PXRD
mutants from the original measurement by ID, location and replacement. It retains
the before/after status and rationale for each; no mutant was suppressed.

## Comparable analytic measurement

Before changing production calculation code, two additional tests independently
derive the triclinic inverse metric, mixed-site interference ratios and cubic
family multiplicities. The source SHA-256 remains
`d344ea26461bf59c6991cb63eb2aabfba363df193c0a998e1b44dd7148a95728`
(normalized LF), matching the historical report. This avoids attributing changed
mutation opportunities to better tests.

The 2026-09-17 run used Node 22.23.2, Stryker 10.0.0, Vitest 4.1.11, accurate
TypeScript checking, the same three-file scope and all 709 generated mutants.
It took 19m47s locally; other milestone validation ran concurrently, so this is
execution cost evidence rather than a controlled speed comparison.

| PXRD status | Historical | Added analytic assertions |
| --- | ---: | ---: |
| Killed | 273 | 290 |
| Survived | 78 | 61 |
| Timeout | 5 | 5 |
| Compile error | 107 | 107 |
| Uncovered / ignored / runtime error | 0 | 0 |
| Score | 78.09% | 82.87% |

Seventeen survivors transitioned to killed. Whole-scope score rose from 82.66%
to 85.79%; formula/search outcomes did not change. Raw reports are retained under
`reports/scientific/mutation-analytic.{json,html}`. The machine-readable review is
generated by `npm run test:mutation:review`, requiring the historical report at
`reports/scientific/mutation-before.json` and the comparable report above.

## Interpretation and final contract

The review distinguishes exact equivalence on reachable triangular bases,
redundant guards, performance-only work, floating-point threshold differences,
malformed-input gaps, incomplete reflection searches, and missing assertions.
Passing tests alone is never the reason for an equivalence decision.

Scientific contract changes belong to #39: strict unsupported-input diagnostics,
neutral-atom form factors, explicit incomplete searches, 10⁻⁷-degree degenerate
grouping and deterministic first-family labels replace several old assumptions.
The old 0.025-degree grouping and strongest-versus-accumulated label decisions
are retired explicitly, not silently called equivalent. New tests also target
coordinate-key collisions and the inclusive upper profile endpoint.

Some exact degeneracy/intensity threshold equalities remain distinguishable in
principle but are outside the independently validated well-conditioned reference
set or below its declared numerical tolerances. Their IDs and reasoning remain
visible; this work does not claim a 100% mutation score, complete numerical proof,
or experimental intensity accuracy. Finite capped searches remain incomplete
even if their tests pass. See the [scientific contract](scientific-validity.md).

The final model's mutation measurement uses the same toolchain but targets
`pxrd.ts` and the new `scattering.ts`; it has a different denominator and must not
be compared as a score improvement over the unchanged-source run. Its raw output
is `reports/scientific/mutation-current.{json,html}`.

That run completed in 28m44s: 891 generated mutants, 503 killed, 13 timeouts,
200 survived, seven uncovered and 168 compile errors (71.37% score). The
[retained report](baselines/pxrd-mutation-current.json) records configuration,
source/input hashes, counts and every surviving or uncovered replacement.
No mutants were ignored. The larger validation and diagnostic surface introduces
new mutation opportunities; the score is advisory and does not certify these
200 survivors as equivalent. Exhaustive classification of this new population
is separate from the 78 historical decisions above.

This retained measurement predates the PR-review correction that extracts rotation
coefficients without subtracting fractional translations. Its hashes identify
that measured source; the added thirds-translation regression is not included in
these mutation counts. The measurement has not been rerun for that correction.

The seven uncovered replacements concern the integer-rotation guard, legacy
nanometre fallback inside symmetry metric validation, and three diagnostic
strings. Some guards repeat prior validation; that alone does not establish all
replacements as equivalent. [Follow-up #78](https://github.com/ogundimo/cif-crystal-data/issues/78)
scopes the new population's triage in the regression protection milestone,
including boundary/diagnostic assertions, without changing tolerances or suppressing mutants.
